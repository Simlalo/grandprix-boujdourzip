# 📦 ملف الانتقال — مشروع الجائزة الكبرى للعدو الريفي بوجدور 2026

> **استخدم هذا الملف كأول رسالة في المحادثة الجديدة مع Claude.**
> أرفقه كاملاً، وضع رسالة قصيرة معه: "هذا ملف انتقال من جلسة سابقة. اقرأه كاملاً قبل الرد."

---

## 🎯 ما هذا المشروع؟

تطبيق ويب لإدارة سباق "الجائزة الكبرى للعدو الريفي والسباق على الطريق" في بوجدور، المغرب.

- **التواريخ:** التصفيات يوم 17 ماي 2026، النهائيات يوم 24 ماي 2026
- **الإنتاج:** https://grandprix.orcept.com (Vercel + Cloudflare DNS)
- **GitHub:** https://github.com/Simlalo/grandprix-boujdourzip
- **Supabase:** project `spatoeqjefkygbiezvgk`
- **التقنيات:** React + Vite + Supabase + RTL Arabic
- **المالك:** محمد لحبيب السملالي (المستخدم super_admin)

---

## 🧭 منهجية العمل المتفق عليها (التزم بها بدقة)

### القاعدة 1: الأمن قبل السرعة
- لا تُغيّر شيئاً قبل التحقق من البنية الفعلية في DB أو الكود
- إذا قال شخص أن X موجود، تحقّق بنفسك (الافتراض الخاطئ كلّفنا ساعات سابقاً)

### القاعدة 2: ملف md لكل تعديل
أي تعديل يُنفّذه المستخدم، يُكتب في ملف `.md` يحتوي:
1. **أمر صارم لـ Replit Agent** في البداية: "لا تُعِد الهيكلة. لا تُنشئ ملفات جديدة. عدّل ملفاً واحداً فقط: X. عند الانتهاء، اسرد الملفات التي عدّلتها"
2. التعديلات المحددة (ابحث عن X، استبدل بـ Y)
3. التحقق المتوقع

### القاعدة 3: المستخدم يعمل الآن محلياً
- المستخدم نزّل المشروع لحاسوبه
- يعمل بـ VSCode + npm run dev
- لذلك **التعديلات اليدوية أفضل من Replit Agent** للملفات الجديدة الطويلة
- بعد كل تعديل ناجح محلياً → push للـ GitHub → Vercel ينشر تلقائياً

### القاعدة 4: ترحيل تدريجي للهيكلة
**هذا قرار جوهري متفق عليه:** كل تعديل وظيفي يُستغل كفرصة لإعادة هيكلة محدودة.
- لا نُعيد هيكلة استباقية
- لا نلمس ملفات لم نعمل عليها هذه الجلسة
- عند العمل على ملف، نستخرج فقط ما له صلة بالتغيير الحالي
- لا نتجاوز 30% من وقت التعديل في إعادة الهيكلة
- المُستخرج له مكان واضح: `src/components/`, `src/lib/`, `src/hooks/`

### القاعدة 5: التواضع المعرفي
- "أنا لا أعرف ما لا أعرف" — اسأل قبل البناء
- handoff documents هي ادعاءات، تحقّق من المصدر
- Replit Agent يميل لإعادة الهيكلة دون إذن

### القاعدة 6: قواعد الذاكرة المحفوظة
المستخدم حفظ هاتين القاعدتين في memory:
- "في بداية أي مشروع جديد أو جلسة استئناف، اسأل عن الغرض والسياق قبل التنفيذ"
- "عند تطبيق منطق تجاري محدد (حسابات، شروط، قواعد)، اعرض الصيغة قبل البناء للتأكيد"

---

## 🗂️ بنية المشروع الحالية

```
src/
├── App.jsx
├── main.jsx (يحتوي BrowserRouter)
├── index.css
├── supabase.js
├── hooks/
│   └── useUserType.js (يكشف committeeMember + institution منفصلين)
├── lib/                      ← أُنشئ في هذه الجلسة (الترحيل التدريجي)
│   └── categories.js          ← ✅ تم
├── components/                ← أُنشئ في هذه الجلسة
│   └── athletes/
│       ├── CategoryCounters.jsx  ← ✅ تم
│       └── AthletesList.jsx       ← ✅ تم
└── pages/
    ├── CommitteeDashboard.jsx
    ├── FinalResultsPanel.jsx
    ├── InstitutionDashboard.jsx (631 سطر، لم يُلمس بعد)
    ├── Login.jsx
    ├── PublicResults.jsx
    ├── RaceDayPanel.jsx
    ├── CallRoomPanel.jsx       ← أُنشئ في جلسة سابقة
    └── components/
        └── CommitteeMembersPanel.jsx (موجود لكن غير مستخدم بعد)
```

---

## 💾 بنية قاعدة البيانات (Supabase)

### الجداول الرئيسية
- **athletes** (24 سجل اختبار حالياً، سيُحذفون)
- **attendance** (تأكيد مزدوج: غرفة النداء + خط الانطلاق)
- **committee_members** (أعضاء اللجنة بأدوارهم)
- **institutions** (5 مؤسسات حالية، تبقى)
- **predefined_educational_institutions**
- **race_rankings** (فارغ)
- **races** (16 سباقاً: 8 تصفيات + 8 نهائي)
- **results** (فارغ)
- **settings**
- **youth_institution_types**
- **institution_standings** (فارغ)

### ENUMs
- `category`: katakit, baraem, sighar, fityan
- `committee_role`: admin, viewer, **super_admin**, **data_entry**, **call_room** (الثلاثة الأخيرة أُضيفت)
- `gender`: male, female
- `institution_type`: education, youth_culture, **free** (الأخيرة أُضيفت)
- `list_status`: draft, submitted, approved, rejected
- `race_stage`: qualifying, final

### بنية attendance (مُحدّثة)
```
id, athlete_id, race_id,
call_room_at TIMESTAMPTZ,
call_room_by UUID,
start_line_at TIMESTAMPTZ,
start_line_by UUID
+ UNIQUE constraint على (athlete_id, race_id)
```

### Functions/Triggers/Policies المهمة
- `compute_category(birth_date)` → generated column
- `detect_duplicate_athlete()` → trigger
- `is_committee_admin()` ← يشمل super_admin
- `is_committee_member()`, `current_institution_id()`
- `admin_create_institution(...)` ← يقبل super_admin
- `super_admin_create_member(p_email, p_password, p_full_name, p_role)`
- `super_admin_assign_role(p_target_user_id, p_role)`
- `super_admin_remove_member(p_member_id)`
- `get_attendance_status(athlete_id, race_id)` → 'verified'|'missing_call_room'|'missing_start_line'|'missing_both'
- 4 سياسات RLS على `attendance`: committee_read, call_room_write, call_room_update, admin_delete

### السباقات (التوقيتات بـ UTC، +1 = توقيت المغرب)
| الفئة | الجنس | المسافة | UTC | المغرب |
|---|---|---|---|---|
| كتاكيت | إناث | 600م | 09:30 | 10:30 |
| كتاكيت | ذكور | 800م | 09:45 | 10:45 |
| براعم | إناث | 1000م | 10:00 | 11:00 |
| براعم | ذكور | 1500م | 10:15 | 11:15 |
| صغار | إناث | 1500م | 10:30 | 11:30 |
| صغار | ذكور | 2000م | 10:50 | 11:50 |
| فتيان | إناث | 2000م | 11:10 | 12:10 |
| فتيان | ذكور | 3000م | 11:30 | 12:30 |

نفس التوقيتات للنهائي (24 ماي).

---

## 👥 الحسابات الإنتاجية الحالية

| البريد | الدور | كلمة المرور | ملاحظات |
|---|---|---|---|
| `simlali@grandprix.ma` | super_admin | `3me&PCj4U76R2Vy` | حساب المالك، UUID: 53cd437f-1448-4e33-9056-89522effb4cf |
| `admin@grandprix.ma` | admin | (مشترك) | حساب أدمن آخر |
| `ahrar@grandprix-boujdour.ma` | admin + مؤسسة "مشاركون أحرار" | `Ahrar2026!` | **دور مزدوج** |
| `Jmanouar@gmail.com` | مؤسسة فقط | (لدى المالك) | إعدادية عمر بن الخطاب |
| `chambredappel@grandprix.com` | call_room | (لدى المالك) | حساب غرفة النداء التجريبي |

---

## ✅ ما أُنجز في هذه الجلسة (9 ماي)

### في DB:
1. ✅ توسيع جدول `attendance` ليدعم التأكيدَين (call_room_at, start_line_at)
2. ✅ 4 سياسات RLS جديدة على attendance
3. ✅ دالة `get_attendance_status`

### في الكود:
1. ✅ إنشاء `CallRoomPanel.jsx` كامل (في جلسة سابقة لكن مذكور للسياق)
2. ✅ تعديل `App.jsx` لتوجيه call_room للشاشة المخصصة
3. ✅ إنشاء `src/lib/categories.js` (ثوابت مشتركة)
4. ✅ إنشاء `src/components/athletes/CategoryCounters.jsx` (8 صناديق العدّادات)
5. ✅ إنشاء `src/components/athletes/AthletesList.jsx` (قائمة + بحث + فلترة)

### اختبارات ناجحة:
- ✅ غرفة النداء: قبول/رفض حسب الوجود + الفئة + المرور بغرفة النداء
- ✅ التأكيد البصري قبل الحفظ يعمل
- ✅ المستخدم اختبر على حاسوب وهاتف

---

## 🎯 المهمة الحالية (التي توقفنا عندها)

**الخطوة 4: تعديل `InstitutionDashboard.jsx` ليستخدم المكوّنين الجديدين**

`InstitutionDashboard.jsx` ملف معقّد (631 سطر) يحتوي:
- نظام `list_status` (draft → submitted → approved/rejected)
- زر "إرسال اللائحة للمصادقة"
- زر "+ إضافة" لرقم الصدرية يدوياً لكل رياضي
- منع التعديل بعد الإرسال
- عرض اسم المستخدم في الـ header
- modal لإضافة رقم الصدرية
- form كبير لإضافة رياضي

**ما يُضاف:** استخدام `<CategoryCounters>` و `<AthletesList>` (المكوّنين الجديدين) + منع تجاوز السقف 10/فئة.

**ما لا يُلمس:**
- ❌ logic إرسال اللائحة (`submitList`)
- ❌ logic إضافة رقم الصدرية يدوياً
- ❌ نظام الـ status
- ❌ form إضافة رياضي

> ⚠️ **ملاحظة عن المستقبل:** نظام `dossard_number` اليدوي سيُلغى لاحقاً (المؤسسة لن تختار الأرقام، اللجنة تُسند مجالات والنظام يُسند تلقائياً). لكن **الآن** نُبقي عليه ولا نلمسه.

> ⚠️ **ملاحظة ثانية:** الإرسال للمصادقة سيصبح **تلقائياً** يوم 13 ماي الساعة 23:00. لكن نُبقي زر "إرسال للمصادقة" اليدوي حالياً.

---

## 📋 المهام المتبقية بترتيب الأولوية

### 🔴 الآن (الجلسة الجديدة):
1. **تعديل `InstitutionDashboard`** لاستخدام المكوّنين الجديدين + منع تجاوز السقف 10
2. **اختبار** (المؤسسة العادية + الأحرار)

### 🟠 قريباً (قبل 17 ماي):
3. **تنظيف الرياضيين الحاليين** (24 سجل اختبار) — جاهز SQL، فقط يحتاج تنفيذ
4. **Trigger في DB** يفرض السقف 10 (الأمان على مستوى الخادم)
5. **ربط `RaceDayPanel` بالحضور الإجباري** (رفض إن لم يجتز التأكيدَين)
6. **تعديل `FinalResultsPanel`** لنظام النقاط الجديد:
   - نقاط الجائزة الكبرى: 10→1 (تصفيات + نهائي) — كما هو
   - نقاط الترتيب الجماعي: 20→1 (تصفيات فقط)
   - شرط الترتيب الجماعي: 4 رياضيين على الأقل من المؤسسة في الـ20 الأوائل
7. **تحديث صفحة النتائج العامة** بنفس المنطق

### 🟡 لاحقاً (قبل 14 ماي):
8. **نظام الصدريات الجديد:**
   - اللجنة تُسند مجالات أرقام للمؤسسات (مثل: 45-60، 115-149)
   - النظام يُسند الأرقام تلقائياً للرياضيين
9. **طباعة بطاقات الصدريات تلقائياً** (PDF)
10. **Cron job أو pg_cron** لإغلاق التسجيل تلقائياً 13 ماي 23:00

### 🔵 مؤجل لـ 2027:
- شاشة المراجعة لـ admin (للنتائج المُعلَّمة)
- Audit log
- Offline-first
- شاشات ميدانية متخصصة لكل دور

---

## 🚨 قرارات حاسمة ثبتت

| القرار | التفاصيل |
|---|---|
| نقاط الجائزة الكبرى | **10 الأوائل، سلم 10→1**. التصفيات والنهائي |
| نقاط الترتيب الجماعي | **20 الأوائل، سلم 20→1**. التصفيات فقط |
| شرط الترتيب الجماعي | المؤسسة تحتاج **4 رياضيين على الأقل** ضمن المراتب 1-20. أقل من ذلك = لا تُحتسب |
| سقف التسجيل | **10 رياضيين/فئة/مؤسسة** (للمدارس فقط) |
| الأحرار | **بدون سقف**. مؤسسة استثنائية (`is_free_participants=true`) |
| التعديل بعد الإغلاق | عبر DB مباشرة فقط (طوارئ، بدون UI) |
| تنظيف البيانات | الرياضيين فقط، المؤسسات تبقى |
| ربط خط الوصول بالحضور | **إجباري** — رفض من لم يجتز التأكيدَين |
| نظام التأكيد المزدوج | **بدون دور call_room جديد لخط الانطلاق**. نفس مسؤول call_room له وضعان (tabs) |

---

## ⚠️ تحذيرات تقنية مهمة

### 1. مشكلة تنسيق الأسطر (CRLF vs LF)
- المستخدم على Windows
- VSCode يحفظ بـ CRLF افتراضياً
- مشروع المستخدم يستخدم Prettier مع LF
- **الحل:** كل ملف جديد، تأكد أن المستخدم يحوّل CRLF→LF (شريط الحالة في VSCode)

### 2. تحذيرات ESLint غير مهمة
- `react/prop-types`: تحذير tutorial، نتجاهله
- `react/react-in-jsx-scope`: نُضيف `import React from 'react'` لإسكاته
- **لا نُضيف `prop-types`** — غير مُثبَّت في المشروع

### 3. مشكلة Replit Git Author المتكررة
عند push من Replit، author = `boujphoenix` يرفضه Vercel.
**الحل:**
```bash
git config user.email "132962137+Simlalo@users.noreply.github.com"
git config user.name "Simlalo"
git commit --amend --reset-author --no-edit
git push --force
```

### 4. Replit Agent
- المستخدم عطّل code optimization
- لا يزال يميل لإعادة الهيكلة
- استخدم تعليمات صارمة جداً في كل ملف md

### 5. الذاكرة الحالية للـ Claude
المستخدم استعمل memory_user_edits ليحفظ:
- كاتب عام في بوجدور + مدير وكالة عقارية Aridel
- صلات بقطاع الصيد البحري في بوجدور
- أمين سر جمعية ألعاب القوى المحلية
- قواعد ذاكرة المشروع (المذكورة في "منهجية العمل")

---

## 📞 للجلسة الجديدة — البدء

عند بدء المحادثة الجديدة:

1. **اقرأ هذا الملف بالكامل** قبل الرد
2. **لا تبدأ بأسئلة "ما الذي تريد فعله؟"** — اقرأ "المهمة الحالية" أعلاه
3. **اسأل المستخدم:** "هل تريد متابعة الخطوة 4 (تعديل InstitutionDashboard) كما خططنا، أم لديك أولوية مختلفة؟"
4. **التزم بمنهجية العمل** المذكورة أعلاه

### ملفات لازمة في الجلسة الجديدة (قد يحتاج المستخدم رفعها):

- `src/pages/InstitutionDashboard.jsx` (الملف الأصلي 631 سطر) — لرؤية الكود قبل أي تعديل
- `src/App.jsx` (لرؤية كيف يستخدم InstitutionDashboard)

### أسلوب التعامل:

- **اللغة:** عربية في النقاش والشرح، الكود بالإنجليزية، رسائل المستخدم النهائية بالعربية
- **التواصل:** مباشر، صادق، مع الإقرار بالأخطاء
- **التحقق:** اسأل قبل افتراض، تحقّق من البنية الفعلية
- **الحجم:** ردود مركّزة، لا إطالة

---

## 🎓 دروس مستفادة من هذه الجلسة (مهم لتجنّب التكرار)

1. **الافتراضات الخاطئة هي العدو** — تحقّق من schema قبل أي تعديل
2. **handoff documents = ادعاءات لا حقائق** — تحقّق من المصدر
3. **استبدال ملف كامل خطر** — لما حاولت استبدال InstitutionDashboard كاملاً، فقدت ميزات لم أكن أدركها (list_status, إرسال للمصادقة, إلخ). **التعديل المُحدّد أأمن**
4. **Replit Agent يُعيد التفسير** — حتى مع أوامر صارمة
5. **عمل المستخدم محلياً (VSCode) أفضل** للملفات الجديدة من Replit Agent

---

**نهاية ملف الانتقال.**
